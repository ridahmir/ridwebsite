# ridahefatima

## Local preview

### Requirements
- Node.js **20.x** (see `.nvmrc`)

### Run
```bash
cd /Users/ridahmir/ridahefatima
npm install
npm run dev
```

Open `http://localhost:3000`.

### If preview ever breaks (safe reset)
```bash
npm run clean
rm -rf .next
npm run dev
```
